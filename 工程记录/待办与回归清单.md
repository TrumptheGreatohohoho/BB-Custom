# 待办与回归清单

汇总 `../docs/` 中所有**"待人工游戏内回归"**项，加上本轮新发现的问题。
每验证一项就把 `[ ]` 改成 `[x]`，并补上日期与证据（日志时间戳 / 截图 / 存档名）。

**证据等级约定**

- `未验证`：没有任何实测证据。
- `弱证据`：2026-08-15 那次会话的日志里没报错 / 该技能被用过，但没有针对性验证。
- `已验证`：有明确的针对性测试和日志或截图证据。

## A. 已知缺陷

- [x] **自定义女性身体缺 `_injured` brush** —— **2026-09-23 已修复、已部署**。
      2026-08-15 日志中 `SceneManager` 请求 `bbca_female_body_01_injured` 失败 9 次；
      修复后 brush 含该刷子名（静态断言 + pack→unpack 逐像素已证）。
      详见 `问题记录/BBCA-女性身体-injured-brush缺失.md`。
- [x] **同源的 `_dead`（尸体）变体** —— 2026-09-23 一并修复。
      `human.nut:117` / `player.nut:956` 的 `<body>_dead` 派生**无** `doesBrushExist` 保护，
      已补 `bbca_female_body_01/02_dead`。
- [x] **`_dead_arrows` / `_dead_javelin`** —— 2026-09-23 判定**不需要补**：
      它们取自 `appearance.Corpse` / `appearance.CorpseArmor`，而 BBCA 从不改写
      `m.Bodies` / `appearance.Corpse`，故始终解析为原版名（已 grep 确认）。
- [ ] **游戏内确认上述修复**（唯一剩余项）：让自定义女性身体角色生命值 < 40% 并阵亡，
      确认 `log.html` 无 `Unknown Brush requested`、受伤与尸体外观正常 —— 未验证。
      **触发步骤、判据与待填结果见 `验收记录/2026-09-23-injured-brush-游戏内验收.md`。**

## B. 技能与装备（docs 中的待验证项）

### 自愈装备（`armor.body.bbca_regenerating_adorned_mail_shirt` / `armor.head.bbca_regenerating_heavy_mail_coif`）

- [ ] 从 Breditor 的 `Legendary` 列表生成两件装备 —— 未验证
- [ ] 受损后每回合恢复 90 —— 未验证
- [ ] 战斗结束后回满 —— 未验证
- [ ] 存读档后数值与外观保持 —— 未验证
- [ ] **蓝羽纹章尖顶盔**：载入**已有**存档后旧头盔立即显示蓝羽（Variant 265）外观，
      新生成装备同样正确（依赖 `onDeserialize()` 迁移 + `updateAppearance()`）—— 未验证
- [ ] 头盔在角色面板 / 库存图标 / 战场立绘三处外观一致 —— 未验证

### Hard Chance（`effects.bbca_hard_chance`，config v1）

- [ ] 战斗外疲劳行显示**正常恢复数值**（不再出现 `0 / 92 (1002)` 这类泄漏）—— 未验证
- [ ] 有 Hard Chance 的角色每回合开始当前疲劳归零 —— 未验证
- [ ] 无 Hard Chance 的角色按原版恢复疲劳 —— 未验证
- [ ] 100% 硬命中 / 100% 硬闪避按配置生效 —— 未验证
- [ ] 角色面板与战术面板无异常、`log.html` 无错误 —— 未验证

### 燃烧手雷（`actives.bbca_fire_grenade`，config v2）

- [ ] `Shift+X` 下拉菜单显示"燃烧手雷 (Fire Grenade)"，可授予并保存 8 项参数 —— 未验证
- [ ] 目标中心 / 第一圈 / 第二圈高亮正确，地图边缘不重复或越界 —— 未验证
- [ ] 空格、敌军占用格、友军占用格均可投掷，占用者按固定友伤结算 —— 未验证
- [ ] 投射期间原目标死亡或移动不改变落点 —— 未验证
- [ ] 水面/不可燃地形、已有烟雾或瘴气、火焰免疫单位行为符合原版 —— 未验证
- [ ] 高度差限制、定身、敌方控制区、投掷专精疲劳折扣、冷却符合配置 —— 未验证
- [ ] 战斗结束后冷却复位；存读档后技能与 v1 参数持续存在 —— 未验证
- [ ] 半径参数（0–4，默认 2；1/7/19/37/61 格）实际生效 —— 未验证

### 召唤装甲复生者（`actives.bbca_summon_zombie`，config v1）

- [ ] 召唤后结束第一回合，第二回合轮到施法者时正常行动，**不再出现 `isNull` 软锁**
      （这是 2026-06-28 的真实故障，必须有针对性复测）—— 未验证
- [ ] 同场有最高级食尸鬼时重复上述流程 —— 未验证
- [ ] 技能栏启用/禁用图标正常，日志无 `gfx/skills/active_26.png` 打开失败 —— 未验证
- [ ] 空格、占用格、不可见格、地图边缘、不同高度目标均可召唤 —— 未验证
- [ ] 召唤物与玩家结盟并由 AI 行动，敌我识别与控制区正常 —— 未验证
- [ ] 达到上限（默认 3，范围 1–12）后技能禁用，召唤物死亡后恢复名额 —— 未验证
- [ ] 多名施法者分别维护各自上限 —— 未验证
- [ ] 召唤物死亡后不自动复活；敌方死灵法师仍可按原版处理尸体 —— 未验证
- [ ] 战斗结束后召唤物及装备不进入佣兵团或战利品 —— 未验证
- [ ] 存读档后 v1 参数持续存在 —— 未验证

### 异常免疫 Aegis（`effects.bbca_negative_immunity_config`，序列化 v6）

- [ ] 默认开项逐项有效：中毒、流血、昏迷、茫然、定身/网、击退/抓取、缴械 —— 未验证
- [ ] 默认关项按需开启后有效：换位、火焰、爆头、伤残、压制、分心、吞噬 —— 未验证
- [ ] 吞噬免疫（`swallow_whole`）：开关关闭时三级食尸鬼仍可吞噬；开启时 AI 不选该目标且
      `skill.use()` 拒绝 —— 未验证
- [ ] 队伍中免疫与非免疫角色并存时 AI 可改选合法目标；全员免疫时普通攻击/移动不受影响 —— 未验证
- [ ] `StunPiercer`：六种原版眩晕攻击可穿透符合条件的硬眩晕免疫 —— 未验证
- [ ] `PassiveCounterattack`：相邻普通主动攻击触发一次反击 —— 未验证
- [ ] `PassiveCounterattack`：**移动/脱离控制区触发的借机攻击不触发反击** —— 未验证
      弱证据：2026-08-15 日志中有 `还击` 使用记录且无报错，但不是针对移动借机攻击的复测
- [ ] 死亡、移位、非相邻、忽略还击路径保持原行为 —— 未验证
- [ ] v6 向后兼容：v1–v5 老存档读取不报错，逐人点"保存参数"后写入 v6 —— 未验证
- [ ] 战斗内技能名与提示为英文 `Aegis`（避免中文字形缺失变方块）—— 未验证

### Shadow Walk

- [ ] AP / 疲劳 / 距离 / 冷却 / 缠斗或定身时可用性等参数可调并生效 —— 未验证
      弱证据：2026-08-15 日志中 `Shadow Walk` 被成功使用 3 次，无报错

## C. 系统级 Hook 与规则

- [ ] **独狼 roster / 部署上限 16**：新开局菜单文案显示 16；新档与已有独狼档可招至 16，
      第 17 人被拒绝；角色界面显示 `16/16`；可部署第 13–16 人；存读档后仍为 16；
      不改敌方规模 —— 未验证
- [ ] **命中率 0–100**：近战与远程分别攻击正在使用 `Shieldwall` 的目标，行为符合预期 —— 未验证
      弱证据：2026-08-15 日志中 `盾墙` 被使用且后续攻击正常，未出现 `IsShieldwallRelevant`
- [ ] 敌方链锤单位走近持盾角色后能正常选择并发动攻击 —— 未验证
- [ ] 被动反击 Hook 读档崩溃已修复：同一存档可正常载入，日志不再有
      `the index 'onAttackOfOpportunity' does not exist` 与 class registration 错误 —— 未验证
      弱证据：2026-08-15 整场会话 0 个 error 行、可存读档
- [ ] 旧命中率活动 ZIP 与旧疲劳恢复 ZIP 均保持 `.bbca-disabled`，活动同哈希 ZIP 数为 0 —— 未验证
- [ ] 历史一体包 `mod_bbca_all_in_one_cn_compat.zip.bbca-disabled` 不再参与加载 —— 未验证
      弱证据：ZIP 条目内该包唯一同路径头盔脚本为 Variant 265

## D. 中文 UI

- [ ] 主菜单按钮为中文 —— 未验证（2026-07-12 曾失败，2026-07-13 才补运行层）
- [ ] 新战役、读档、角色、物品、事件、城镇、战斗与 tooltip 文本为中文 —— 未验证
- [ ] `TranslateDialog` / `TranslateButtons` / `TranslateTooltips` / `TranslateTownScreenNames`
      均无 `is not defined`，无黑屏与卡死 —— 未验证
      弱证据：2026-08-15 日志中 `Transl` 出现 0 次，即无相关报错
- [ ] Breditor 加载顺序为 `world_names.js` → `ui.js` → `bbca_cn_ui_compat.js` → `mod_hooks.js` —— 未验证
- [ ] `Shift+X` 编辑器面板本身为中文 —— 未验证

## E. 环境与流程

- [ ] 安装前确认 `BattleBrothers.exe` 未运行（**每次安装的硬前置**）—— 2026-09-23 两轮核对均为未运行
- [ ] 5 个 `.bbca-backup` 哈希与 `当前状态.md` 第 4 节基线一致 —— 2026-09-23 修复前后均逐项核对一致
- [ ] 每次会话结束 `git status` 干净且 `main == origin/main` —— 2026-09-23 为干净 + `0 0`

## E2. 静态验收（不需要进游戏，2026-09-23 已全部通过）

一条命令跑完：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\assert_custom_appearance_pack.ps1
```

结果：**70 passed, 0 failed**（退出码 0）。覆盖范围与对应勾选：

- [x] 派生 brush：brush 含 `bbca_female_body_01/02_injured` + `_dead`，刷子名共 18 个
- [x] catalog 仍 14 项、不含任何 `_injured`/`_dead`（面板不被污染，`bbca_isCatalogBrush` 不接受）
- [x] 独狼：hook 恰一份、`BrothersMax = 16`、`BrothersMaxInCombat = 16`、调用原版 `onInit`、不碰 `BrothersScaleMax`
- [x] Hard Chance：`setFatigue(0)` 保留、无 `FatigueRecoveryRate`、无 `forcedRecovery`
- [x] Aegis：`StunPiercer` / `PassiveCounterattack` 存在、config `writeI32(6)` 且保留 v1–v5 兼容分支
- [x] 被动反击：用 `mods_hookExactClass`，且不再有会崩档的 `mods_hookClass("entity/tactical/actor")`
- [x] 命中率：无 `IsShieldwallRelevant`、经 `mods_queue` 注册 `mod_bbca_hitchance`、保留偏射参数
- [x] 燃烧手雷：`AreaRadius` 动态读取、config `writeI32(2)`、v1 默认 2
- [x] 召唤：`zombie_yeoman`、`PlayerAnimals`、`ResurrectionChance = 0`、两张自有图标已打包
- [x] 自愈装备：稳定 ID、270 耐久、头盔 Variant 265 + `onDeserialize` 迁移 + `updateAppearance`、无旧 237
- [x] 汉化：`world_names.js` / `ui.js` / `bbca_cn_ui_compat.js` 随包；Breditor 加载顺序
      `world_names → ui → bbca_cn_ui_compat → mod_hooks`
- [x] Steam 包 SHA-256 与构建一致、`.bbca-backup` 数量为 5
- [x] pack → unpack 往返：18 个精灵逐像素与原图一致；原有 14 个几何漂移 0

**这个脚本只证明"包内容与设计一致"，不能替代游戏内回归。**

## F. 下一轮建议的验证顺序（性价比排序）

1. ~~修 `_injured` brush~~ —— **已完成（2026-09-23）**，只剩游戏内目视确认。
2. 独狼 16 人上限（一个存档界面就能确认，`16/16`）。
3. Hard Chance 疲劳（截图即可判断是否还泄漏 `(1002)`）。
4. 自愈装备（生成 → 受伤 → 回合恢复 → 战后回满 → 存读档，一条链）。
5. 召唤僵尸第二回合 `isNull`（历史上真软锁过，优先）。
6. 中文 UI 全量走查（主菜单 → 读档 → 大地图 → 城镇 → 战斗）。
7. 燃烧手雷 8 参数与半径。
8. Aegis 逐项 + 被动反击移动过滤。
