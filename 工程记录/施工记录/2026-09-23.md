# 施工记录 2026-09-23

## 任务

用户要求：查看当前工程状态；`D:\project\BB-Custom\工程记录` 是记忆区与施工记录区，当前为空，
看有什么可以写入的。

## 做了什么（只读勘察 + 建档，未改源码、未构建、未写 Steam）

1. 读工程入口与记忆索引：`README.md`、`START-HERE.md`、`docs/README.md`、
   `docs/current-state.md`（全文）、`docs/engineering-log.md`（首尾）、`docs/git-sync.md`、
   `docs/png-asset-spec.md`。
2. 核对 git：HEAD、提交时间、`main...origin/main`、工作区状态、remote。
3. 核对构建与部署：`build/custom_appearance/mod_bb_custom_appearance.zip` 与 Steam
   `data\mod_bb_custom_appearance.zip` 的大小与 SHA-256；汉化运行层哈希；ZIP 条目清单。
4. 清点 Steam `data\`：活动 ZIP、8 个 `.bbca-disabled`、5 个 `.bbca-backup`（逐个取哈希）。
5. 定位日志与存档（发现与文档不一致，见下），解析 2026-08-15 会话日志的 511 行结构化内容。
6. 建立本目录结构与 5 个文件。

## 发现

### 1. 工程 / 构建 / 部署三者一致，不需要动作

- `main == origin/main == 1c42cb5`（2026-07-14 01:39），工作区干净。
- 构建 ZIP 与 Steam 已装 ZIP **同哈希** `30AD22B4...09CB`（194156 字节），与
  `docs/current-state.md` 最后一条记录一致；汉化运行层 `0EAF5F0B...B647` 亦一致。
- 结论：docs 的部署记录未过期，本机没有被文档之外的改动污染。

### 2. `docs/` 说的日志路径在本机不成立（文档偏差）

多篇文档写 `%USERPROFILE%\Documents\Battle Brothers\log.html`。实测本机
`C:\Users` 下**任何**用户的 `Documents\Battle Brothers` 都不存在；
真正的日志在**游戏安装根目录** `<游戏目录>\log.html`，存档在 `<游戏目录>\savegames\`。

已写入 `当前状态.md` 第 7 节。**建议**（未执行，需用户确认后再动 `docs/`）：
把 `docs/current-state.md` 的"下次接手先检查"第 4 条和 `START-HERE.md` 第 5 节里
引用 `log.html` 的位置改为"游戏安装目录 `log.html`（本机实测；`Documents` 路径不成立）"。

### 3. 新缺陷：自定义女性身体缺 `_injured` brush

2026-08-15 会话日志中 `SceneManager` 请求 `bbca_female_body_01_injured` **失败 9 次**。
已从原版反编译代码定位派生路径（`character_background.nut:407` 由当前 body brush 名派生），
并确认 `asset_repo` 与构建 ZIP 中都不存在任何 `injured` 条目。
详见 `问题记录/BBCA-女性身体-injured-brush缺失.md`。

### 4. 一批"待人工回归"项从来没有被确认过

`docs/current-state.md` 里散落着大量"待人工游戏回归"段落（自愈装备、Hard Chance 面板疲劳、
燃烧手雷、召唤僵尸、Aegis v6、被动反击、独狼 16、中文 UI 等）。本次把它们汇总成可勾选清单，
放在 `待办与回归清单.md`，并标注证据等级。

### 5. 最近一次真实游戏会话是 2026-08-15（距本次核对约 5 周）

`_20736.sav` @ 2026-08-15 13:26:24，会话 13:18:10→13:26:26 正常关闭；
511 行日志中 **0 个 error 级**，33 条 warning 里 **24 条是预期噪音**（21 条 `data\` 非官方
文件抱怨 + 1 条 "modified files" + 1 条云同步 vdf + 1 条 "modified version" 存档提示），
剩下 **9 条同属一个真实问题**（见发现 3）。
当场用到并正常：`Shadow Walk`、`还击`、`盾墙`、`投掷投斧`，自定义外观图集正常加载卸载。

## 没有做（边界）

- 没有修改任何源码、资产、manifest 或 `docs/` 下任何文件。
- 没有重新构建，没有运行任何安装脚本，没有向 Steam 写入任何内容。
- 没有删除 / 覆盖 / 移动任何 `.bbca-backup` 或 `.bbca-disabled`。
- 没有动 `data\` 里用户自有的 4 个第三方 OEM ZIP。
- 本目录内容**未提交**：按 `docs/git-sync.md`，离开本机前需要 `git add -A` + commit + push。

## 下次先做什么

1. 决定是否修 `_injured` brush（见 `问题记录/`，改动局限在 `asset_repo` + manifest + 重建）。
2. 决定是否按发现 2 修正 `docs/` 里的日志路径描述。
3. 从 `待办与回归清单.md` 的 F 节按顺序做游戏内回归，优先独狼 16 与 Hard Chance 疲劳
   （截图即可判定）。
4. 把本目录 commit + push，让另一台电脑也能读到这份记忆。

---

# 第二轮（同日）：执行三项收尾

用户指示"三条一起做，自行 loop 验收，我暂时离开"，并补充"做不了的事（如画图）跳过"。

## 结果总览

| 项 | 状态 |
| --- | --- |
| 1. 修 `_injured` brush | **完成并部署**（并顺带修了同源的 `_dead`） |
| 2. 修 `docs/` 日志与存档路径 | **完成**（4 处指导性文档 + 追加式记录） |
| 3. 回归验收 | **不需要人工的部分全部跑完（70 项断言 0 失败 + 往返验证）**；纯游戏内项目仍待人工 |

## 第 1 项：派生 brush 修复

**关键结论：不需要画图。** 追查发现上游已有现成素材，且 BBCA 的身体本来就是 FantasyBro 的资源：

- 逐像素比对确认 `bbca_female_body_01` = FantasyBro `bust_naked_body_7869`、
  `bbca_female_body_02` = `bust_naked_body_7870`（差异像素均为 **0**），
  且 FantasyBro 的 `metadata.xml` 几何/`ic` 与 manifest 逐字段一致。
- FantasyBro **自己**就把同一张 `bust_naked_body_7870_injured.png` 复用给 10 个身体
  （含 `7869_injured`）；该图与**原版 `bust_naked_body_02_injured.png` 字节相同**，
  而 `_dead` 用的是 FantasyBro 自有尸图。
- 追查中还发现**同一根因的第二处**：`human.nut:117` 与 `player.nut:956` 会派生
  `<当前 body brush>_dead` 且**没有** `doesBrushExist` 保护，所以自定义身体角色阵亡时
  也会缺刷子。一并修复。
- 判定**不需要**补 `_dead_arrows` / `_dead_javelin`：它们取自 `appearance.Corpse` /
  `appearance.CorpseArmor`，而 BBCA 从不改写 `m.Bodies` / `appearance.Corpse`（已 grep 确认）。

**改动**（4 个文件）

1. `asset_repo/.../sprites/bodies/` 新增 4 张 PNG（injured 80×60、dead 131×110）。
2. `asset_repo/custom_appearance/manifest.json`：新增 4 个 sprite，几何/`ic` 照抄 FantasyBro。
3. `tools/build_custom_appearance_pack.ps1`：新增 `hidden` 支持 —— 隐藏 sprite **进 brush
   但不进 `::BBCA_Catalog`**。这是必需的：后端 `bbca_isCatalogBrush()` 用 catalog 校验用户
   APPLY，派生刷子若进 catalog 就会出现在 `Shift+X` 面板并可被当普通外观应用。
4. `docs/png-asset-spec.md`：新增"派生 brush 与 `hidden` 标记"契约小节。

**构建与部署**：build → 245140 字节，SHA-256 `2E6975CA...2DA39`；生成 `.nut` 通过
disposable `bbsq.exe -e`；按常设授权用标准安装器部署。安装前后 `BattleBrothers.exe` 未运行，
Steam 与构建同哈希，5 个 `.bbca-backup` 逐项哈希**安装前后完全一致**。

## 第 2 项：日志与存档路径

`docs/` 把日志写成 `%USERPROFILE%\Documents\Battle Brothers\log.html`，实测本机该目录不存在。
已改的**指导性**文档（保留历史条目不动）：

- `docs/development-playbook.md` 第 6 节 —— 改为"两处按顺序找、取最新"，并写明缺失路径不等于日志干净。
- `docs/current-state.md` 环境节 + "下次接手先检查"第 4 条 + 火焰手雷节的一处引用。
- `codex-skills/.../SKILL.md` 第 45 行。
- `docs/engineering-log.md` **追加** 2026-09-23 条目（遵守其"不删旧证据"规则）。

## 第 3 项：验收（loop）

**新增可重复工具** `tools/assert_custom_appearance_pack.ps1`（并写入 development-playbook）：

- 读取最终 ZIP，断言派生 brush、catalog 项数与可见性、独狼 16、Hard Chance 归一化、
  Aegis v6 与各 Hook 的 exact-class 写法、命中率内嵌、燃烧手雷 v2、召唤 v1、两件自愈装备、
  汉化资源、Breditor 加载顺序、Steam 哈希与备份数量。失败即非零退出。
- **70 passed, 0 failed。**
- 过程中我自己的脚本报过 8 项失败，全部是**脚本 bug**而非包缺陷：
  `ZipArchive.GetEntry()` 是精确匹配而包内用反斜杠分隔（改为规范化匹配），
  以及序列化版本号的实际写法是 `_out.writeI32(6)`（我原先猜的是 `SerializationVersion = 6`）。
  两处都已修正；修正后才作为结论采用。

**pack → unpack 往返验证**（替代一部分无法自动化的游戏内验证）：

- 把最终 brush 解包回来：18 个精灵**逐像素**与 manifest 原图一致。
  字节不一致的 14 个是 bbrusher 重编码 PNG 所致，**不是**内容差异。
- 与 2026-07-10 旧构建的解包结果逐字段比对：原有 14 个精灵几何**漂移 0**，
  只新增 4 个 id、无删除 —— 证明本次改动对既有资产零回归。
- 顺带记录一个**先于本次改动就存在**的现象：`bbca_female_hair_05` 的 `left`/`right`
  在 manifest 里是 `-30`/`30`，但 bbrusher 打包后写成空值；旧构建完全相同，故非本次回归，
  未追查。

**明确没做的事（附理由）**：没有启动游戏做启动冒烟测试。理由有两层：(1) 主菜单启动**根本不会**
触发本次修复的代码路径（brush 只在战术外观/受伤/阵亡时解析），信息增量接近零；
(2) 无人值守启动会牵动 Steam Cloud 存档同步，而无法自动做任何游戏内操作，
风险大于收益。纯游戏内项目已如实保留为"未验证"。

## 本轮边界

- 未删除/覆盖/移动任何 `.bbca-backup` 或 `.bbca-disabled`（安装前后逐项哈希已比对）。
- 未触碰用户自有的 4 个第三方 ZIP。
- 未修改任何技能/数值/序列化字段 —— 本次只动资产与构建脚本。

## 推送（第二轮末）

- 首次 `git push` 失败：GitHub 从本机不可达（`Test-NetConnection github.com -Port 443` = `False`，
  `curl 28` / 连接重置）。按 `docs/git-sync.md` 停手，未做 force 或任何绕过，提交留在本地。
- 用户挂上 VPN 后仍是"浏览器能上、git 上不了"。定位到本机 **WestWorldVPN** 把 WinINET
  系统代理设为 `127.0.0.1:21882`（另有 SOCKS5 `21881`），两个虚拟网卡始终 Disconnected；
  git 的 libcurl 不读 WinINET，且这两个本地代理对 git TLS 不可用
  （`schannel: failed to receive handshake`）。
- **VPN 起来后直连是通的**，于是显式清空代理推送成功：
  `git -c http.proxy= -c https.proxy= push` → `1c42cb5..23c2531  main -> main`，`0 0` 同步。
- 该注意事项已写入 `当前状态.md` 第 2 节，避免下次重复踩坑。

## 回写（第二轮末，用户去游戏验证前）

按用户要求做了一次收尾回写，全部基于**重新实测**的事实：

- 重跑 `tools/assert_custom_appearance_pack.ps1` → **70 passed, 0 failed**（退出码 0）；
  `git` 为 `0 0` 且工作区干净；`log.html` 的 `<title>` 再次确认游戏版本 `1.5.2.3`。
- `docs/current-state.md`：把顶部"最近核对"从 `2026-07-12` 更新为 `2026-09-23`，
  并注明游戏版本是当日重新确认的。
- 新建 `工程记录/验收记录/`，首个文件是
  `2026-09-23-injured-brush-游戏内验收.md`：写清触发条件（HP < 40% / 阵亡）、
  5 条通过判据、要抓的失败字符串、核日志的命令，以及**待填的结果表**，
  让用户回报后可以一次填完，不用重新推导。
- `README.md`（本目录索引）、`待办与回归清单.md` A 节、`问题记录/` 第 6 节都已指向该验收文件。
- 明确区分了**预期噪音**（21 条 `Unexpected file or directory` 等）与**真正的失败信号**
  （`Unknown Brush requested` / `Script Error`），避免用户把正常噪音当成没修好。
